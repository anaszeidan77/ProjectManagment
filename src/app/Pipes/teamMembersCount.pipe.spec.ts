/* tslint:disable:no-unused-variable */

import { TeamMembersCountPipe } from './teamMembersCount.pipe';

describe('Pipe: TeamMembersCounte', () => {
  it('create an instance', () => {
    let pipe = new TeamMembersCountPipe();
    expect(pipe).toBeTruthy();
  });
});
